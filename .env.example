# JICTurbo Environment Variables
# Copy this file to .env and update with your values
# NEVER commit the .env file to version control!

# ===================================
# DATABASE CONFIGURATION
# ===================================
POSTGRES_DB=jicturbo
POSTGRES_USER=jicturbo
POSTGRES_PASSWORD=your_secure_database_password_here_change_this

# ===================================
# JWT AUTHENTICATION
# ===================================
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_change_this
JWT_EXPIRES_IN=7d

# ===================================
# SMTP EMAIL CONFIGURATION
# ===================================
# For Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your_gmail_app_password_here
SMTP_FROM="Your CRM Name <your-email@gmail.com>"

# ===================================
# APPLICATION URLS
# ===================================
# Frontend URL (used by backend for CORS and email links)
FRONTEND_URL=https://your-frontend-domain.com
FRONTEND_DOMAIN=your-frontend-domain.com

# Backend API URL (used by frontend to connect to API)
BACKEND_DOMAIN=api.your-backend-domain.com
BACKEND_URL=https://api.your-backend-domain.com/api

# ===================================
# DOCKER CONFIGURATION
# ===================================
DOCKER_USERNAME=your-dockerhub-username
DOCKER_HUB_TOKEN=your_dockerhub_access_token

# ===================================
# NOTES
# ===================================
# 1. Generate a strong JWT_SECRET: openssl rand -base64 32
# 2. Use Gmail App Passwords for SMTP_PASS (not your regular password)
# 3. Update all domains to match your setup
# 4. Ensure Traefik is configured with letsencryptresolver for SSL
